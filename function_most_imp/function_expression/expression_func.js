//funciton expression is a another way of defining function in this we define function inside a varible
//and to call the function we use variable name

//they are also called as nameless function
const sum = function (a, b) {
  //output on console - sum(10,10)
  return a + b;
};

//
const name = function () {
  console.log("Athara kesarkar"); //outpur on console - name();
};

//IMPORTANT
// If I want a function to behave like a variable and do everything a variable can do, I will use a function expression.
